# Optional local data

The default public configuration uses FRED-only data and does not require a
local file.

To extend the High Yield OAS history, place the source workbook in this folder
and update the `local_data` section in `config.yaml`. Local source files are
excluded from Git and should not be redistributed without permission.
